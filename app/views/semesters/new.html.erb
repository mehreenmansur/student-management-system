<%= form_for @semester do |f| %>
    </br>
    <p>
      <%= f.label :semester_name %><br>
      <%= f.text_field :name %>
      <%= f.hidden_field :status, :value => "upcoming" %><br><br>
    </p>

    <button type="submit" class="btn btn-default">Create Semester</button>

<% end %>